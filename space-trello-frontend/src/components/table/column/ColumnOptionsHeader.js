import React from "react";
import FlexRow from "../../helper/FlexRow";

const ColumnOptionsHeader = ({ children }) => {
  return <FlexRow className="pt-2 justify-content-center">{children}</FlexRow>;
};

export default ColumnOptionsHeader;
