import React from "react";

const CardIconContainer = ({ children }) => {
  return (
    <div style={{ width: 50 }} className="h-100">
      {children}
    </div>
  );
};

export default CardIconContainer;
